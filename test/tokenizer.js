// 'use strict';

// var chai        = require( 'chai' ),
//     //sinon       = require( 'sinon' ),
//     //sinon_chai  = require( 'sinon-chai' ),
//     Tokenizer     = require( '../src/tokenizer' ),

//     expect      = chai.expect;

// //chai.use( sinon_chai );

// describe( 'Tokenizer', function(){
//     var target = {
//         foo: {
//             bar: [
//                 {
//                     qux: function( value ){
//                         return {
//                             baz: value
//                         };
//                     }
//                 },
//                 {
//                     qux: function( value ){
//                         return {
//                             baz: value
//                         };
//                     }
//                 }
//             ]
//         }
//     };
    
    it( 'should parse', function(){
        /*
        var tokenizer = new Tokenizer();
=======
//     it( 'should parse', function(){
//         var tokenizer = new Tokenizer();
>>>>>>> f65f97b6c46a1c8409bcee72f49fe802e81ac0ba
        
//         tokenizer.addRule( 'segment', /^[a-zA-Z0-9]+\.$/ );
        
//         tokenizer.addRule( 'function', /^[a-zA-Z]+\(.*\)$/ );
        
//         tokenizer.addRule( 'array', /^[a-zA-Z]+\[.+\]$/ );
        
//         tokenizer.on( 'token', function( token, id ){
//             console.log( 'TOKEN', token, id );
//         } );
        
        tokenizer.tokenize( 'foo.bar.100.qux.baz' );
        tokenizer.flush();
        tokenizer.tokenize( 'foo.bar[100]qux()baz' );
        */
        //expect( keyPath.get( 'foo.bar.100.qux.baz' ) ).to.be.undefined;
        //expect( keyPath.get( 'foo.bar[100]qux(123)baz' ) ).to.equal( 123 );
    } );
} );
//         tokenizer.tokenize( 'foo.bar.100.qux.baz' );
//         tokenizer.flush();
//         tokenizer.tokenize( 'foo.bar[100]qux()baz' );
        
//         //expect( keyPath.get( 'foo.bar.100.qux.baz' ) ).to.be.undefined;
//         //expect( keyPath.get( 'foo.bar[100]qux(123)baz' ) ).to.equal( 123 );
//     } );
// } );
